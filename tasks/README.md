# Tasks

`todo.json` is the single source of truth for tasks.

Use the Dashboard todo panel for normal updates. Keep related work grouped into task packages instead of splitting one project into many tiny neighboring tasks.
